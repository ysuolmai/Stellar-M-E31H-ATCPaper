适配设备：汉朔Stellar-M E31H【主板HS_EL5102_1M_62_04】（注：本项目延用Stellar-L3N的改编内容，自动适配分辨率和设备不一定仍可生效，不建议刷入2.13寸屏幕外其他尺寸的屏幕）

您可以选择直接去releases页面下载已经编译好的.bin文件，单文件体积小下载速度快

固件对无LED设备较为友好，兼容下可以运行

网页管理和刷机页面地址：https://lightning1003.github.io/index.html 本仓库中的网页管理对参考项目进行了重构，内容主要如下： 1.加入了自动换行自动适应屏幕的文本编辑功能，直接以类似备忘录方式添加文本 2.提供是否发送红色数据选项，默认只发送黑白数据，使用黑白渲染，速度更快。-M E31H没有红色色层，硬件上适合纯黑白 3.优化了界面UI，使管理页面易于使用且更加美观 4.图片渲染默认居中裁切，不进行拉伸 5.底部可折叠日志区，适配移动端使用场景

备用上位机管理地址：https://javabin.cn/stellar-L3N-etag/web_tools 固件尽可能保证了兼容性，使用备用地址的L3N项目上位机也可以管理

如果你把锂电池接到了设备 VCC，可编译 `PROFILE=lipo` 版本。TLSR8258 会将空闲的 PB7 输出为与 VCC 相同的高电平并通过 ADC 采样，固件再按 3.2V=0%、4.2V=100% 估算电量。如果电池先经过稳压芯片，VCC 始终固定在 3.3V 左右，则软件无法从 VCC 反推出电池剩余电量。直接使用最高 4.2V 的锂电池供电前，也必须确认整机和 MCU 的允许供电范围。

设备端固件主要优化： 1.时钟采用顶部公历日期、星期和电量，中间大号时间，底部地支生肖、农历、当天节气和温度的布局；时钟页不显示 BLE 状态 2.屏幕经过验证的局部波形会损坏变化中的数码管笔画，因此时钟每分钟完整刷新，优先保证数字完整 3.固件通过 PB7 采样 VCC；标准版按2.2V到3.1V估算原电池电量，VCC LiPo版按3.2V到4.2V估算锂电池电量 4.农历和节气支持 2000-2099 年，生肖按农历年切换

## 自动编译和发布

每次推送到 `main` 后，GitHub Actions 会编译标准版和 LiPo 版，并在工作流的 Artifacts 中提供下载。推送 `v` 开头的标签（例如 `v1.0.0`）会自动创建 GitHub Release，附带两个固件和 SHA256 校验文件。

本地编译命令：

```bash
make -C Firmware clean all
make -C Firmware clean all PROFILE=lipo
```

当前界面显示的温度来自电子纸控制器内部温度传感器，不是 MCU 内部 ADC 温度。该温度更适合电子纸波形补偿，只能近似反映环境温度。

首次上电和之后每分钟都使用完整刷新；通过管理页面同步时间或切换时钟模式时也会立即全屏刷新。设备没有后备 RTC，VCC 完全断电后时间会恢复为固件初始值，需要重新连接并同步时间。

详细硬件分类和教程： 【电子价签开发-汉朔Stellar-M系列 - CSDN App】https://blog.csdn.net/2501_92943976/article/details/163269392?sharetype=blog&shareId=163269392&sharerefer=APP&sharesource=2501_92943976&sharefrom=link 这是我根据Stellar-M系列价签硬件的总结和烧录过程写的博客可供参考

附加内容：（详细信息查看CSDN链接） 烧录教程 ##（只写TLSR8359的设备）

###1.连接 准备工具：CH340G串口模块（普通款有TX，RX，3V3，GND就行，自动复位反而不如手动操作），杜邦线（母）若干

将杜邦线剪开，一头留铜丝，一头是母口 按照图片中的次序飞线： 飞线线序图（仓库里面image里面有） 其中除了NRST飞的杜邦线另一端留公口用于手动短接，其他的都留母口直接插到CH340G模块的插针上

###2.开始烧录

电脑端打开网页烧录工具，必须用Chrome或者Edge

https://atc1441.github.io/ATC_TLSR_Paper_UART_Flasher.html

可以用我的优化版工具： https://lightning1003.github.io/uart_flasher.html UI我重新进行了布局，界面更好看

把CH340G接到电脑上，随后在网页上点击“Open”，在弹出的窗口选择CH340G的COM号（不知道的可以右键开始-设备管理器-端口，里面看看CH340G device后面是COM多少），连接后点击“选择文件”，找到编译好的固件.bin文件

说明：固件.bin文件需要在你先运行github下载的zip包的Firmware文件夹里面的makeeit.exe编译获取，程序跑完后你就会在这个firmware文件夹里面找到ATC-Paper.bin了。如果本来firmware里面就有这个文件建议删掉后重新编译

选择.bin文件打开后： ##注意：烧录完成后必须拔掉其余接线只留下3V3和GND供电！不然会一直处在烧录模式

手动方案：拿起NRST上面的那根线，把线捅在GND插针上来短接NRST和GND，此时价签会停止串口检测通信强行初始化关机。松开的瞬间就点击“Unlock Flash”（解锁闪存），等待完成解锁后，再点击“Write To Flash”（写入闪存），随后等待烧录完成，完成后日志中出现“soft reset MCU”之后价签会自动刷新进入新固件工作，如果没动静请先尝试用手机搜索蓝牙，如果蓝牙也找不到设备说明要么当时短接时操作失误写入失败，要么你是用错了@和不带@版本的固件导致死机。

自动方案：直接NRST接到RTS，不用操作短接，其余和手动一样

烧录完成后可以使用对应固件的网页管理工具来初始化时间和更改内容。
